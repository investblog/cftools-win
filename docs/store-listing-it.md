# Cloudflare Tools — Store Listing (Italiano / it)

## Description

Cloudflare® è un marchio registrato di Cloudflare, Inc. Cloudflare Tools è un'app indipendente e open source di 301.st. Non è affiliata a Cloudflare, Inc., né approvata o sponsorizzata da essa. Funziona con il tuo account Cloudflare tramite l'API pubblica Cloudflare v4.

L'interfaccia dell'app è in inglese.

Cloudflare Tools porta la gestione massiva delle zone, rapida e affidabile, sul desktop Windows. Pensata per i professionisti dei domini che gestiscono decine o centinaia di zone su più account Cloudflare.

• Aggiunta domini in blocco — incolla domini da qualsiasi fonte: elenchi, CSV, HTML, e-mail, URL o esportazioni. Il parser estrae i domini radice, gestisce IDN e Punycode, segnala i duplicati e ti lascia controllare l'elenco prima di creare qualcosa.
• Elenco zone ed esportazione CSV — ogni zona con stato, piano e name server; filtra per nome ed esporta in CSV, per un account o per tutti gli account insieme.
• Svuotamento cache in blocco — seleziona le zone, avvia lo svuotamento e segui l'avanzamento zona per zona.
• Eliminazione zone in blocco — filtra, seleziona, conferma in una finestra di sicurezza, segui lo stato di ogni zona; l'elenco si ricarica al termine.
• Esportazione dei risultati — dopo ogni aggiunta, svuotamento o eliminazione, salva un CSV con stato e testo dell'errore per dominio.
• Token API o Global API Key — accedi con un token utente (cfut_), un token dell'account (cfat_) o la chiave classica; il tipo viene riconosciuto dal segreto incollato.
• Più account — accedi una volta e cambia account senza reinserire le credenziali.
• Tema scuro e chiaro — secondo Windows o a scelta.

Le credenziali restano in Gestione credenziali di Windows e vengono inviate solo all'API Cloudflare. Nessuna telemetria, nessuna analisi, nessun acquisto. Codice sorgente su GitHub.

Disponibile anche come estensione per Chrome, Edge e Firefox con lo stesso nome. Creata da 301.st, una piattaforma di gestione dei redirect edge.

## What's new

Versione 1.2.0:
• Pagina Zone con esportazione CSV (un account o tutti)
• Accesso con token API (cfut_ / cfat_) oltre alla Global API Key
• Esportazione dei risultati del batch (aggiunta / svuotamento / eliminazione) in CSV
• «Valuta l'app», link alle estensioni e nota sul marchio nella pagina Informazioni

## Product features (keyword-first, ≤200 chars each)

1. Aggiunta domini in blocco a Cloudflare: incolla elenchi, CSV, HTML o URL — domini radice estratti, IDN e Punycode gestiti, duplicati segnalati, controllo prima della creazione
2. Elenco zone con stato, piano e name server — filtro per nome, esportazione CSV per un account o tutti gli account insieme
3. Svuotamento cache in blocco per le zone selezionate, con avanzamento per zona e annullamento
4. Eliminazione zone in blocco con conferma di sicurezza, stato per zona e ricaricamento automatico
5. Esportazione dei risultati del batch in CSV: dominio, stato, errore — per archivio o per un nuovo tentativo
6. Accesso con token API (cfut_ / cfat_) o Global API Key, tipo riconosciuto automaticamente; i segreti restano in Gestione credenziali di Windows
7. Più account: un solo accesso, cambio di account Cloudflare senza reinserire le credenziali
8. Coda di richieste che rispetta i limiti, con tentativi e backoff; tema scuro e chiaro; open source, senza telemetria

## Search terms (Store — max 7 terms, 30 chars each)

cloudflare in blocco, aggiungere domini, svuotare cache, eliminare zone, esportare zone csv, token api cloudflare, zone dns
